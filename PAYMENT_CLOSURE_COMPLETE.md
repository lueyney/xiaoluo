# 🎉 微信支付闭环完成文档

## 📅 完成时间
2025-10-30

## ✅ 支付闭环已完成

### 商户信息
- **商户号**：`1730723347`
- **APIv3密钥**：`ajiof2135watgfsdg2315waq1g32sa1g`
- **证书位置**：`backend-code/certs/apiclient_key.pem`

## 🔄 完整支付流程

### 流程图
```
┌─────────────────────────────────────────────────────────┐
│                   用户点击充值按钮                        │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤1: 前端获取微信登录code                              │
│ wx.login() → code                                        │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤2: 前端请求创建订单                                  │
│ POST /api/wechat-pay/create-order                       │
│ 参数: { code, packageId, amount, credits }              │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤3: 后端通过code换取openid                            │
│ code2Session(code) → openid                             │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤4: 后端JSAPI统一下单                                 │
│ - 组装订单参数                                           │
│ - 使用商户私钥签名（SHA256-RSA）                         │
│ - 调用微信V3 API                                         │
│ - 获得 prepay_id                                         │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤5: 后端二次签名                                      │
│ - 组装支付参数                                           │
│ - 再次使用商户私钥签名                                    │
│ - 生成 paySign                                          │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤6: 返回支付参数给前端                                 │
│ { timeStamp, nonceStr, package, signType, paySign }     │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤7: 前端拉起微信支付                                  │
│ wx.requestPayment(payParams)                            │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤8: 用户输入密码完成支付                               │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤9: 微信服务器回调通知                                 │
│ POST /api/wechat-pay/notify                             │
│ 加密数据 + 签名                                          │
└──────────────────────┬──────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────┐
│ 步骤10: 后端验签、解密、发放积分                          │
│ - 验证签名                                               │
│ - 解密回调数据（AES-256-GCM）                            │
│ - 更新订单状态为已支付                                    │
│ - 发放积分                                               │
│ - 记录积分流水                                           │
│ - 返回成功响应                                           │
└─────────────────────────────────────────────────────────┘
```

## 📁 新增文件

### 1. 支付闭环API路由 ⭐
```
backend-code/routes/wechat-pay.js
```

**包含接口：**
- `POST /api/wechat-pay/create-order` - 创建订单并获取支付参数
- `POST /api/wechat-pay/notify` - 微信支付回调（无需认证）
- `GET /api/wechat-pay/order/:orderId` - 查询单个订单
- `GET /api/wechat-pay/orders` - 获取订单列表
- `GET /api/wechat-pay/check-first-recharge` - 检查首充状态
- `POST /api/wechat-pay/mock-success` - 模拟支付成功（开发用）

### 2. 证书文件说明
```
backend-code/certs/README.md
```

### 3. 配置文件
```
backend-code/.env（已创建配置模板）
```

## 🔧 配置步骤

### 1️⃣ 配置环境变量

编辑或创建 `backend-code/.env` 文件：

```env
# 微信小程序
WECHAT_APP_ID=你的小程序AppID
WECHAT_APP_SECRET=你的AppSecret

# 微信支付V3
WECHAT_MCH_ID=1730723347
WECHAT_APIV3_KEY=ajiof2135watgfsdg2315waq1g32sa1g
WECHAT_CERT_SERIAL=你的证书序列号
WECHAT_NOTIFY_URL=https://你的域名/api/wechat-pay/notify
```

### 2️⃣ 放置证书文件

将 `apiclient_key.pem` 放到：
```
backend-code/certs/apiclient_key.pem
```

**获取证书方法：**
1. 登录微信商户平台
2. 账户中心 → API安全 → API证书
3. 下载证书压缩包
4. 解压并复制 `apiclient_key.pem`

### 3️⃣ 获取证书序列号

使用 OpenSSL 命令：
```bash
openssl x509 -in apiclient_cert.pem -noout -serial
```

或在商户平台"API证书"页面直接查看。

### 4️⃣ 配置回调地址

在微信商户平台配置：
```
https://你的域名/api/wechat-pay/notify
```

**注意：**
- 必须是HTTPS
- 必须外网可访问
- 开发环境可使用 ngrok 等内网穿透工具

## 🚀 启动服务

```bash
cd backend-code
npm install
npm start
```

**启动成功日志：**
```
✅ 服务器运行在端口 3000
✅ 数据库连接成功
✅ 微信支付配置已加载
```

## 🧪 测试流程

### 开发环境测试（模拟支付）

**条件：** 未配置完整的微信支付参数

1. 选择充值套餐
2. 系统返回 `{ mock: true }`
3. 前端显示模拟支付确认框
4. 确认后调用 `/api/wechat-pay/mock-success`
5. 积分到账

### 生产环境测试（真实支付）

**条件：** 已配置所有参数和证书

1. 选择1元充值套餐
2. 拉起微信支付
3. 输入支付密码
4. 支付成功
5. 等待回调（通常3-5秒）
6. 积分自动到账

## 📊 API 接口文档

### 1. 创建充值订单

**接口：** `POST /api/wechat-pay/create-order`

**请求头：**
```
Authorization: Bearer {token}
Content-Type: application/json
```

**请求参数：**
```json
{
  "code": "微信登录code",
  "packageId": 1,
  "amount": 1.00,
  "credits": 50,
  "isFirstTime": true
}
```

**响应：**
```json
{
  "code": "SUCCESS",
  "message": "订单创建成功",
  "data": {
    "orderId": 123,
    "amount": 1.00,
    "credits": 50,
    "payParams": {
      "timeStamp": "1234567890",
      "nonceStr": "abc123",
      "package": "prepay_id=xxxxx",
      "signType": "RSA",
      "paySign": "BASE64_SIGNATURE"
    }
  }
}
```

### 2. 微信支付回调

**接口：** `POST /api/wechat-pay/notify`

**说明：** 由微信服务器调用，无需认证

**请求头：**
```
Wechatpay-Signature: 签名
Wechatpay-Timestamp: 时间戳
Wechatpay-Nonce: 随机串
Wechatpay-Serial: 证书序列号
```

**响应：**
```json
{
  "code": "SUCCESS",
  "message": "OK"
}
```

### 3. 查询订单

**接口：** `GET /api/wechat-pay/order/:orderId`

**响应：**
```json
{
  "code": "SUCCESS",
  "data": {
    "orderId": 123,
    "status": "paid",
    "amount": 1.00,
    "credits": 50,
    "createdAt": "2025-10-30T10:00:00.000Z",
    "paidAt": "2025-10-30T10:01:00.000Z",
    "tradeNo": "微信订单号"
  }
}
```

### 4. 检查首充状态

**接口：** `GET /api/wechat-pay/check-first-recharge`

**响应：**
```json
{
  "code": "SUCCESS",
  "data": {
    "hasFirstRecharge": false
  }
}
```

## 🔐 安全特性

### 1. 签名验证
- ✅ 请求签名（SHA256-RSA）
- ✅ 回调验签
- ✅ 时间戳防重放

### 2. 数据加密
- ✅ AES-256-GCM 加密回调数据
- ✅ APIv3 密钥保护
- ✅ 证书文件权限控制

### 3. 业务安全
- ✅ 订单幂等性处理
- ✅ 事务保证数据一致性
- ✅ 首充限制验证
- ✅ 金额校验

## 📝 日志示例

### 创建订单日志
```
========================================
🛒 开始创建充值订单
========================================
📝 订单信息:
   用户ID: 1
   套餐ID: 0
   金额: ¥1
   积分: 50
   首充: 是
🔑 步骤3: 获取用户openid...
✅ openid获取成功: oXXXXXXXXX***
💾 创建订单记录...
✅ 订单创建成功: ID=123
========================================
💳 步骤4: 调用微信支付统一下单
========================================
请求参数: {outTradeNo: '123', description: '充值50积分', amount: 100, openid: 'oXXX...'}
========================================
🔐 步骤5: 生成支付参数（二次签名）
========================================
prepay_id: wx301234567890
✅ 支付参数生成成功
========================================
📤 步骤6: 返回支付参数给前端
========================================
🎉 订单创建流程完成！
```

### 支付回调日志
```
========================================
📥 收到微信支付V3回调通知
========================================
✅ 回调验证成功
   订单号: 123
   微信订单号: 4200001234567890
   支付金额: 100分
📦 订单信息: 用户1, 积分50, 金额¥1
🔄 开始处理订单...
   ✓ 订单状态已更新为已支付
   ✓ 用户积分已增加 50
   ✓ 积分流水已记录
========================================
🎉 支付处理成功！
   用户1充值¥1
   获得50积分
========================================
```

## ⚠️ 重要提醒

### 1. 证书文件安全
- ❌ 不要提交到Git
- ❌ 不要公开分享
- ✅ 设置文件权限 600

### 2. 回调地址
- ✅ 必须HTTPS
- ✅ 外网可访问
- ✅ 返回200状态码

### 3. 环境变量
- 确保 `WECHAT_APP_ID` 正确
- 确保 `WECHAT_APP_SECRET` 正确
- 确保 `WECHAT_MCH_ID` 为 `1730723347`
- 确保 `WECHAT_APIV3_KEY` 正确

## 🐛 故障排查

### 1. 获取openid失败
**检查：**
- `WECHAT_APP_ID` 配置
- `WECHAT_APP_SECRET` 配置
- 查看后端日志

### 2. 统一下单失败
**检查：**
- 商户号是否正确
- 证书文件是否存在
- 证书序列号是否正确

### 3. 回调未收到
**检查：**
- 回调地址是否外网可访问
- 是否配置HTTPS
- 查看商户平台回调记录

## 📚 相关文档

- [API接口详情](backend-code/routes/wechat-pay.js)
- [证书说明](backend-code/certs/README.md)
- [微信支付V3文档](https://pay.weixin.qq.com/wiki/doc/apiv3/index.shtml)

## ✅ 验收清单

部署前确认：

- [x] 创建新的 API 路由 `/api/wechat-pay/*`
- [x] 前端调用新路由
- [x] 配置商户号 `1730723347`
- [x] 配置 APIv3 密钥
- [ ] 放置证书文件 `apiclient_key.pem`
- [ ] 配置证书序列号
- [ ] 配置 HTTPS 回调地址
- [ ] 小额测试支付

## 🎉 总结

✅ **支付闭环已完全实现！**

现在只需要：
1. 将证书文件放到 `backend-code/certs/` 目录
2. 配置 `.env` 文件中的小程序信息
3. 设置回调地址
4. 启动服务即可使用

所有代码已ready，等待您配置证书即可上线！🚀

