# 🔧 修复 404 错误

## ❌ 问题

访问以下API时返回404错误：
```
GET  /api/wechat-pay/check-first-recharge  → 404
POST /api/wechat-pay/create-order          → 404
```

## ✅ 原因

后端服务器还在运行旧代码，新创建的路由文件需要重启服务器才能生效。

## 🔧 解决方法

### 步骤1：停止当前服务

在运行后端的终端中：
- 按 `Ctrl + C` 停止服务

或者在Windows任务管理器中：
- 找到 `node.exe` 进程
- 结束进程

### 步骤2：重启服务

```bash
cd backend-code
npm start
```

### 步骤3：验证

服务启动后，应该看到：
```
✅ 服务器运行在端口 3000
✅ 数据库连接成功
✅ 微信支付配置已加载
```

### 步骤4：测试API

再次访问充值页面，应该不会有404错误了。

## 📊 验证路由加载

服务启动后，可以在浏览器访问：
```
http://127.0.0.1:3000/health
```

应该返回：
```json
{
  "status": "ok",
  "timestamp": "...",
  "uptime": ...
}
```

## 🎯 正确的API路由

确保前端调用的是新路由：

### 检查首充
```
GET /api/wechat-pay/check-first-recharge
```

### 创建订单
```
POST /api/wechat-pay/create-order
```

### 支付回调
```
POST /api/wechat-pay/notify
```

## 📝 检查点

重启后检查：

1. ✅ 后端控制台无错误
2. ✅ 前端可以正常访问
3. ✅ 不再出现404错误
4. ✅ 日志中有详细的支付流程记录

## 🐛 如果仍然404

### 检查1：文件是否存在
```bash
cd backend-code
dir routes\wechat-pay.js
```

应该看到文件存在。

### 检查2：app.js是否正确引入
打开 `backend-code/app.js`，确认有：
```javascript
const wechatPayRoutes = require('./routes/wechat-pay');
app.use('/api/wechat-pay', wechatPayRoutes);
```

### 检查3：端口是否被占用
```bash
# Windows
netstat -ano | findstr :3000

# 如果端口被占用，结束进程
taskkill /PID <进程ID> /F
```

### 检查4：查看错误日志
```bash
cd backend-code
type logs\error.log
```

## ✅ 完成

重启后，所有路由应该正常工作：

```
✅ /api/wechat-pay/check-first-recharge
✅ /api/wechat-pay/create-order
✅ /api/wechat-pay/notify
✅ /api/wechat-pay/order/:id
✅ /api/wechat-pay/orders
✅ /api/wechat-pay/mock-success
```

---

**重启服务后问题即可解决！** 🚀

